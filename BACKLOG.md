# MyFlow バックログ

コードレビュー（2026-07-23 時点 / commit `9b422e8` ベース）で洗い出した課題一覧。
※ 実行環境（Supabaseキー）が無いため、**実機操作ではなくコード精査**による起票です。再現確認が必要なものは「要再現確認」と明記しています。

優先度: **P1**=早めに直したい（不具合/セキュリティ） / **P2**=改善したい / **P3**=あると良い

| ID | 種別 | 優先 | タイトル | 状態 |
|----|------|------|----------|------|
| [#1](#1) | 不具合 | P1 | 日付判定がUTC基準で、繰り返しタスクが午前9時までリセットされない | ✅ 完了 |
| [#2](#2) | セキュリティ | P1 | APIに認証が無く、URLを知っていれば誰でも全データを読み書きできる | 保留（方針検討中） |
| [#3](#3) | 不具合 | P1 | 保存の成否を確認しておらず、失敗しても画面上は成功に見える | ✅ 完了 |
| [#4](#4) | 不具合 | P2 | 通信失敗時に前のモードのデータが残り、別モードの内容が表示される | ✅ 完了 |
| [#5](#5) | 不具合 | P2 | 目標追加でエラー時に壊れた行が一覧に混入する | ✅ 完了 |
| [#6](#6) | 不具合 | P2 | ドラッグ中断（pointercancel）でスクロールが効かなくなる | ✅ 完了 |
| [#7](#7) | 不具合 | P2 | 追加ボタン連打でタスクが重複作成される | ✅ 完了 |
| [#8](#8) | 性能 | P2 | 並び替え1回でタスク数ぶんのAPIリクエストが飛ぶ | ✅ 完了 |
| [#9](#9) | 性能 | P2 | タスク追加のたびに全データ（4エンドポイント）を再取得している | ✅ 完了 |
| [#10](#10) | 性能 | P3 | 状態更新のたびに全タスク行が再レンダリングされる | 未着手 |
| [#11](#11) | 不具合 | P3 | 未完了/完了で sort_order が重複し、並び順が崩れることがある | ✅ 完了 |
| [#12](#12) | 性能 | P3 | 子タスク探索が O(n²) になっている | ✅ 完了 |
| [#13](#13) | UX | P3 | エラー通知が alert() でUXが悪い | ✅ 完了 |
| [#14](#14) | UX | P3 | 折りたたみ状態・完了フィルタが保存されない | ✅ 完了 |
| [#15](#15) | 保守 | P3 | AI削除後の不要コード・依存が残っている | ✅ 完了 |
| [#16](#16) | 保守 | P3 | 一覧APIに取得件数の上限が無い | ✅ 完了 |
| [#17](#17) | a11y | P3 | 絵文字だけのボタンにアクセシブルな名前が無い | ✅ 完了 |

---

<a id="1"></a>
## #1 日付判定がUTC基準で、繰り返しタスクが午前9時までリセットされない
**種別**: 不具合 / **優先度**: P1 / **影響**: 繰り返しタスク、日付表示

### 現象
`today()` が UTC の日付を返すため、日本時間（UTC+9）では **0:00〜8:59 の間、前日の日付**を返します。

```ts
// app/page.tsx
function today() { return new Date().toISOString().split("T")[0] }  // ← UTC
```

結果として:
- 繰り返しタスクの「完了」が**日付をまたいでも午前9時までリセットされない**（`isTaskDone` が `recurring_done_date === today()` で判定）
- `nextRecurrenceDate()` が UTC日付とローカル曜日（`getDay()`）を混在させて計算している
- 一方 `isOverdue()` / `fmtDue()` は**ローカル時刻**基準のため、`today()` と食い違う

### 再現手順
1. 繰り返しタスクを夜に完了する
2. 翌日の朝8時にアプリを開く → まだ「完了」のままで未完了に戻らない

### 修正案
ローカル日付を返すよう統一する。

```ts
function today() {
  const d = new Date()
  const m = String(d.getMonth() + 1).padStart(2, "0")
  const day = String(d.getDate()).padStart(2, "0")
  return `${d.getFullYear()}-${m}-${day}`
}
```
併せて日付を扱う箇所（`nextRecurrenceDate`）がローカル基準で一貫しているか確認する。

---

<a id="2"></a>
## #2 APIに認証が無く、URLを知っていれば誰でも全データを読み書きできる
**種別**: セキュリティ / **優先度**: P1

### 現象
- 全APIルートが `const USER_ID = "default"` 固定で、リクエスト元を検証していない
- `lib/db.ts` は **service_role キー**でSupabaseに接続するため RLS を素通りする
- 結果、デプロイURLを知っていれば誰でも `GET/POST/PATCH/DELETE /api/tasks` 等を実行できる

`.env.local.example` に NextAuth(GitHub) の設定項目はあるが、**実装されていない**（`app/login` も未使用）。

### 検討状況
- **2026-07-23**: パスコード（共有パスワード）方式で一度実装したが、**採用しない方針**となったため取り消し。
  同じ提案は繰り返さないこと。

### 修正案（未定）
1. NextAuth を導入し、全APIルートでセッション検証を行う
2. `USER_ID` をセッションのユーザーIDに置き換える（既存データの `user_id: "default"` は移行が必要）
3. 併せて Supabase の RLS 有効化を検討

※ 個人利用でURLが非公開なら緊急度は下がるが、**データ全消しが誰でも可能**な状態である点は認識しておきたい。

---

<a id="3"></a>
## #3 保存の成否を確認しておらず、失敗しても画面上は成功に見える
**種別**: 不具合 / **優先度**: P1

### 現象
楽観的更新（先に画面を更新→あとでAPI送信）の後、**レスポンスを一切確認していない**箇所が多数あります。

対象: `toggleTask` / `saveTaskText` / `saveTaskMemo` / `saveEditModal` / `deleteTask` / `reorderTasks` / `updateGoalPct` / `saveGoalEdit` / `deleteGoal` / `deleteMemo` / `saveHighlight`

```ts
// 例: app/page.tsx saveTaskText
setTasks(prev => ...)          // 画面は更新される
await fetch(..., { method: "PATCH", ... })   // ← 失敗しても何も起きない
```

そのため通信エラーやDBエラー時に**画面とDBが黙って食い違い**、リロードで変更が消えます。
（実際、以前あった「詳細が消える」不具合もこの構造が原因で発覚が遅れました）

### 修正案
- 共通ラッパを作り、`res.ok` を検査して失敗時はロールバック＋トースト通知
```ts
async function patchTask(id: string, body: object, rollback: () => void) {
  const res = await fetch(`/api/tasks/${id}`, { method: "PATCH", headers: {...}, body: JSON.stringify(body) })
  if (!res.ok) { rollback(); notify("保存に失敗しました") }
}
```

---

<a id="4"></a>
## #4 通信失敗時に前のモードのデータが残り、別モードの内容が表示される
**種別**: 不具合 / **優先度**: P2

### 現象
`fetchAll` は `Promise.all` が失敗すると `catch` でログ出力のみ行い、**state を更新しません**。

```ts
catch (e) { console.error(e) }
finally { setLoading(false) }
```

仕事→プライベートに切り替えた瞬間に通信が失敗すると、**仕事モードのタスクがプライベートとして表示され続けます**（ユーザーには区別がつかない）。しかもエラー表示が無い。

### 修正案
- 失敗時は `setTasks([])` 等でクリアし、「読み込みに失敗しました（再試行）」を表示する
- モード切替時にリクエストのレースを防ぐ（`AbortController` か最新リクエストIDの判定）

---

<a id="5"></a>
## #5 目標追加でエラー時に壊れた行が一覧に混入する
**種別**: 不具合 / **優先度**: P2

### 現象
`addGoal` が `res.ok` を確認せず、レスポンスJSONをそのまま一覧へ追加しています。

```ts
const res = await fetch("/api/goals", {...})
const goal: Goal = await res.json()   // 失敗時は { error: "..." }
setGoals(prev => [goal, ...prev])     // ← 壊れた行が入る
```

APIが500を返すと `{ error }` が `Goal` として追加され、名前が空・`pct` が `undefined` の行が表示され、`avgGoal` の平均計算も `NaN` になります。

### 修正案
`res.ok` と `goal.id` を検査し、失敗時はエラー表示して追加しない。（メモ側は対応済み）

---

<a id="6"></a>
## #6 ドラッグ中断（pointercancel）でスクロールが効かなくなる
**種別**: 不具合 / **優先度**: P2 / **要再現確認**（スマホ実機）

### 現象
長押しドラッグ実装は `pointerup` でのみ後始末をしています。

```ts
window.addEventListener("pointermove", onWinMove)
window.addEventListener("pointerup", onWinUp)
// pointercancel は未処理
```

一方、ドラッグ中はページスクロールを止めるため `touchmove` を `preventDefault` しています。
```ts
const handler = (ev: TouchEvent) => { if (draggingRef.current) ev.preventDefault() }
```

OSのジェスチャや通知などで **`pointercancel` が発生して `pointerup` が来ない**と、`draggingRef` が立ったまま残り、**ページが一切スクロールできなくなる**恐れがあります（リロードするまで復帰しない）。

### 修正案
- `pointercancel` でも `onWinUp` と同じ後始末を行う
- コンポーネントのアンマウント時にもリスナ解除と `draggingRef` リセット（現状リークもある）

---

<a id="7"></a>
## #7 追加ボタン連打でタスクが重複作成される
**種別**: 不具合 / **優先度**: P2

### 現象
`handleAddMainTask` は `await addTask()`（内部でPOST→`fetchAll`）を待つ間、**ボタンを無効化していません**。入力欄も `defaultValue` の非制御入力のため、素早く2回Enter/クリックすると同じ内容で2件作成されます。サブタスク追加も同様。

### 修正案
送信中フラグ（`submitting`）でボタン/Enterを無効化し、送信直後に入力欄をクリアする。

---

<a id="8"></a>
## #8 並び替え1回でタスク数ぶんのAPIリクエストが飛ぶ
**種別**: 性能 / **優先度**: P2

### 現象
`reorderTasks` は表示中の全親タスクに連番を振り直し、**全件をPATCH**しています。

```ts
await Promise.all(newIds.map((id, i) =>
  fetch(`/api/tasks/${id}`, { method: "PATCH", body: JSON.stringify({ sort_order: i }) })
))
```

タスクが30件あればドラッグ1回で**30リクエスト**。件数が増えるほど遅く、失敗も検知していません（#3）。

### 修正案
- 変更が必要な範囲だけ更新する、または
- `sort_order` を小数にして**移動した1件だけ**を前後の中間値に設定する（例: `(prev+next)/2`）
- もしくは一括更新エンドポイントを用意する

---

<a id="9"></a>
## #9 タスク追加のたびに全データ（4エンドポイント）を再取得している
**種別**: 性能 / **優先度**: P2

### 現象
`addTask` は成功後に必ず `await fetchAll()` を呼び、**tasks / goals / memos / highlights の4本**を取り直しています。サブタスク1件の追加でも同様。

### 修正案
POSTのレスポンス（作成されたタスク）を `setTasks(prev => [...])` で差し込む。整合が不安な場合も `tasks` のみの再取得に留める。

---

<a id="10"></a>
## #10 状態更新のたびに全タスク行が再レンダリングされる
**種別**: 性能 / **優先度**: P3

> **訂正（2026-07-23）**: 当初「メモ入力やタスク名編集の1文字ごとに発生」と記載したが**誤り**。
> `InlineTextEdit` / `TaskMemoEditor` / `ChildTaskInput` は各自の内部stateで入力を保持しているため、
> **打鍵では Home は再レンダリングされない**。実際に発生するのは完了トグルや追加・削除などの操作時のみ。
> 影響は限定的なため P2 → P3 に引き下げ。

### 現象
`ctx` オブジェクトを毎レンダーで新規生成しているため、`TaskItem` の props が毎回変わり、
状態更新のたびに**全行が再レンダリング**されます。数十件では体感差は出ませんが、件数が増えると効いてきます。

```ts
const ctx: TaskCtx = { childrenMap, mode, editingId, ... }   // 毎回新しい参照
```

### 修正案
- ハンドラを `useCallback`、`ctx` を `useMemo` で安定化
- `TaskItem` を `React.memo` でラップ
- さらに、編集中テキストのstateを行コンポーネント内に閉じる（現状は既に一部対応済み）

---

<a id="11"></a>
## #11 未完了/完了で sort_order が重複し、並び順が崩れることがある
**種別**: 不具合 / **優先度**: P3

### 現象
`reorderTasks` は**表示中（未完了 or 完了）のタスクにだけ** 0,1,2… を振ります。そのため未完了側と完了側で同じ `sort_order` 値が重複し、タスクを完了→未完了に戻したときに意図しない位置へ入ることがあります。

### 修正案
並び替え時は完了/未完了を問わず全親タスクを対象に採番する、または #8 の小数方式に切り替える。

---

<a id="12"></a>
## #12 子タスク探索が O(n²) になっている
**種別**: 性能 / **優先度**: P3

### 現象
`TaskItem` が毎回 `ctx.tasks.filter(t => t.parent_id === task.id)` を実行するため、タスク数 n に対して O(n²) の走査になります。数十件なら問題ありませんが、増えると効いてきます。

### 修正案
`parent_id → 子配列` の Map を親側で1度だけ構築し、`ctx` 経由で渡す。

---

<a id="13"></a>
## #13 エラー通知が alert() でUXが悪い
**種別**: UX / **優先度**: P3

`addTask` などの失敗時に `alert()` を使っており、操作がブロックされ見た目も不揃いです。控えめなトースト表示に統一したい。

---

<a id="14"></a>
## #14 折りたたみ状態・完了フィルタが保存されない
**種別**: UX / **優先度**: P3

並び順（`sortMode`）は localStorage に保存されるようになりましたが、**折りたたみ状態（`collapsedIds`）と未完了/完了フィルタ（`filter`）はリロードで初期化**されます。挙動が不揃いなので揃えたい。

---

<a id="15"></a>
## #15 AI削除後の不要コード・依存が残っている
**種別**: 保守 / **優先度**: P3

- `package.json` の `@anthropic-ai/sdk` が未使用のまま（インストール時間・容量の無駄）
- `highlights.month` を取得しているが「今月の目標」削除により**未使用**
- タスクPATCH APIが `mode` 更新を受け付けるが、モード移動機能は削除済み
- `app/login/` と `.env.local.example` の NextAuth 項目が未使用（#2 で実装するなら残す）
- `app/globals.css` の `.memo-grid` はメモUI刷新により未使用
- `app/api/debug` が本番に残っている（不要なら削除）

---

<a id="16"></a>
## #16 一覧APIに取得件数の上限が無い
**種別**: 保守 / **優先度**: P3

`db.select` は常に全件取得（`order=created_at.desc` のみ）で、`limit`/ページングがありません。タスクやメモが数百件を超えると初期表示が重くなります。将来的にページングか上限の導入を検討。

なお `db.select` は `order=created_at.desc` を固定で付与するため、**`created_at` 列を持たないテーブルではエラー**になります（新テーブル追加時の落とし穴）。

---

<a id="17"></a>
## #17 絵文字だけのボタンにアクセシブルな名前が無い
**種別**: a11y / **優先度**: P3

`✏️` `📝` `＋` `×` などのボタンは `title` はあるものの `aria-label` が無く、スクリーンリーダーでは用途が伝わりません。`aria-label` を付与したい。

---

## 完了済み（参考）
- 詳細（優先度・期限）がPATCHで保存されず消える不具合
- 入力中にフォーカスが外れる不具合（`TaskItem` のコンポーネント再生成）
- 繰り返しタスクの期限超過表示（「次回」表示へ変更）
- スマホでのタスク行レイアウト崩れ
